<!DOCTYPE html>
<%= render 'shared/header' %>

<div class="yourwaste">
	You are looking to throw away -
	<%= @waste_query %>
	<p>
		<strong>This is a type of - </strong>
		<%= @type_of_waste %>
		<%= image_tag(@waste_logo, style: "height: 70px; width: 70px") %>
	</p>
</div>

<p class="schedulelink">
	<%=  link_to "My waste pick up schedule", {controller: "schedule", action: "show"}, {class: "schedulelinktext"} %>
</p>