<!DOCTYPE html>
<% content_for :body_class, "small-background" %>

<%= render 'shared/header' %>

<%= render 'shared/searchbar' %>

<div class="yourwaste">
	We couldn't search for -
	<strong>"<%= @waste_query %>"</strong>. Although, you can refer to <%= link_to("Types of Waste", types_of_waste_path, {:class => "link-text"} ) %> to categorise the waste yourself.
	<br>
	You can also check your waste pick up schedule <%= link_to("here", {controller: "schedule", action: "show"}, {:class => "link-text"}) %>.
</div>
<%= render 'shared/howToGetRidOfWaste'%>